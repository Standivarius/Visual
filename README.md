# Visual

`Visual` is the canonical local repository and current product name for Doxa's Windows low-vision magnification application.

The application itself lives under `app/`.

Supporting research, specifications, experiments, datasets, artifacts, and development infrastructure are deliberately kept outside `app/` so experimental work does not silently become production architecture.

Project-wide agent and engineering rules are defined in [`AGENTS.md`](AGENTS.md).
